# ========= Core Server =========
# Cổng backend (khuyên dùng 3000 cho dev)
PORT=3000

# Frontend origin để CORS (dev ai dùng port nào thì sửa ở .env.local của người đó)
CLIENT_ORIGIN=http://localhost:5173

# ========= Public URL cho OAuth (địa chỉ công khai của BACKEND) =========
# Dev: http://localhost:3000
# Prod: https://stylesnap.your-domain.com  (nếu đặt API dưới subdomain) 
#  hoặc https://your-domain.com            (nếu reverse proxy API tại /api)
SERVER_PUBLIC_URL=http://localhost:3000

# ========= Google OAuth =========
# Lấy từ Google Cloud Console (Dev & Prod dùng client khác nhau)
GOOGLE_CLIENT_ID=382358951251-g10usk3di6hj68ud3th9iv5o91hbr80d.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# Callback luôn trỏ về BACKEND (không trỏ FE)
# Dev:  http://localhost:3000/api/auth/google/callback
# Prod: https://your-domain.com/api/auth/google/callback     (nếu API path-based)
#       https://api.your-domain.com/api/auth/google/callback (nếu API subdomain)
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# ========= Session Cookie =========
# Không dùng DB sessions/JWT. Cookie phiên tự ký bằng SECRET này.
SESSION_SECRET=change_me_dev
# Số ngày sống của phiên
SESSION_DAYS=7
# Nếu API chạy ở subdomain, cần cookie dùng chung domain cha:
# SESSION_COOKIE_DOMAIN=.your-domain.com

# ========= SQL Server =========
DB_HOST=localhost
DB_PORT=1433
DB_NAME=stylesnap
DB_USER=sa
DB_PASS=12345678
